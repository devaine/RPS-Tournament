{
  "globalDependencies": ["tsconfig.json"],
  "ui": "stream",
  "tasks": {
    "build": {
			"dependsOn": ["^build"],
			"inputs": ["apps/frontend", "apps/api"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
		"host": {
			"dependsOn": ["^build"]
    }
  },
  "remoteCache": {}
}
