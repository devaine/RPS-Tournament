{
  "globalDependencies": ["tsconfig.json"],
  "ui": "stream",
  "tasks": {
		"build": {
			"dependsOn": ["^build"],
			"inputs": ["apps/frontend", "apps/api"],
			"outputs": ["dist"],
			"cache": false,
			"persistent": false
		},
		"dev": {
			"cache": false,
			"persistent": true
		},
		"host": {
			"dependsOn": ["^build"],
			"inputs": ["apps/api/dist"]
		}
  },
  "remoteCache": {}
}
