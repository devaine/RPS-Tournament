services:
  rps:
    image: cicd-registry.devdoes.work/rps-developers/rps-tournament:cicd-${CI_COMMIT_SHORT_SHA}

    ports:
      - 5176:5173

    deploy:
      update_config:
        order: start-first

        #secrets:
        #  rps-port:
        #    external: true
